.kpis { display: flex; gap: 16rpx; }
.kpi { flex: 1; text-align: center; padding: 24rpx 12rpx; }
.small { font-size: 44rpx; }
.map-wrap { height: 52vh; min-height: 480rpx; }
.map { width: 100%; height: 100%; }
.dot { width: 20rpx; height: 20rpx; border-radius: 50%; flex-shrink: 0; }

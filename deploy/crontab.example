# 每小时触发一次简报检查；接口内部按各旅程时区判断是否该发
# 安装：crontab -e 后粘贴（把端口和 CRON_SECRET 换成实际值）
0 * * * * curl -fsS -m 110 -H "Authorization: Bearer YOUR_CRON_SECRET" http://127.0.0.1:3100/api/cron/briefing >/dev/null 2>&1
